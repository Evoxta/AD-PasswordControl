<Window x:Class="EvoxtaADReset.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available"
        Height="620"
        Width="560"
        MinHeight="620"
        MinWidth="560"
        Background="#0f172a"
        Foreground="#f8fafc"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="30">

        <Border Background="#1e293b"
                CornerRadius="12"
                Padding="22">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="An update is available!"
                           FontSize="22"
                           FontWeight="Bold"/>

                <TextBlock Grid.Row="1"
                           Name="VersionText"
                           Margin="0 6 0 14"
                           Foreground="#94a3b8"/>

                <TextBlock Grid.Row="2"
                           Text="Update Notes"
                           FontWeight="Bold"
                           Margin="0 0 0 6"/>

                <Border Grid.Row="3"
                        Background="#0f172a"
                        CornerRadius="8"
                        Padding="8"
                        MinHeight="300"
                        Margin="0 0 0 14">

                    <WebBrowser Name="UpdateNotesBrowser"/>
                </Border>

                <TextBlock Grid.Row="4"
                           Name="StatusText"
                           Margin="0 0 0 12"
                           Foreground="#94a3b8"
                           Text="Ready to install update."/>

                <StackPanel Grid.Row="5"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">

                    <Button Name="LaterButton"
                            Content="Later"
                            Width="100"
                            Height="36"
                            Margin="0 0 10 0"
                            Click="Later_Click"/>

                    <Button Name="UpdateNowButton"
                            Content="Update Now"
                            Width="120"
                            Height="36"
                            Click="UpdateNow_Click"/>
                </StackPanel>
            </Grid>

        </Border>

    </Grid>

</Window>
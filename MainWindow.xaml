<Window x:Class="EvoxtaADReset.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password Control"
        Height="560"
        Width="780"
        Background="#0f172a"
        FontFamily="Segoe UI"
        Foreground="#f8fafc">

    <Window.Resources>
    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="#f8fafc"/>
        <Setter Property="Background" Value="#1e293b"/>
        <Setter Property="BorderBrush" Value="#334155"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Height" Value="36"/>
    </Style>
    <Style TargetType="PasswordBox">
        <Setter Property="Foreground" Value="#f8fafc"/>
        <Setter Property="Background" Value="#1e293b"/>
        <Setter Property="BorderBrush" Value="#334155"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Height" Value="36"/>
    </Style>
    <Style TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#6366f1"/>
        <Setter Property="BorderBrush" Value="#6366f1"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="#f8fafc"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

</Window.Resources>
    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>

            <TextBlock Text="Password Control"
                       FontSize="24"
                       FontWeight="Bold"/>

            <Button Content="Help"
                    HorizontalAlignment="Right"
                    Width="70"
                    Click="Help_Click"/>

        </Grid>

        <TextBlock Name="DomainText"
                   Grid.Row="0"
                   Margin="0 32 0 0"
                   Foreground="#94a3b8"/>

        <StackPanel Grid.Row="1"
                    Margin="0 20 0 10">

            <TextBlock Text="Username"/>

            <TextBox Name="UsernameBox"
                     LostFocus="UsernameBox_LostFocus"/>

        </StackPanel>

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border Background="#1e293b"
                    CornerRadius="12"
                    Padding="18"
                    Margin="0 0 10 0">

                <StackPanel>

                    <TextBlock Text="User Information"
                               FontWeight="Bold"
                               FontSize="16"
                               Margin="0 0 0 10"/>

                    <TextBlock Name="FirstNameText"/>
                    <TextBlock Name="LastNameText"/>
                    <TextBlock Name="DisplayNameText"/>
                    <TextBlock Name="LogonNameText"/>
                    <TextBlock Name="AccountStatusText"/>

                </StackPanel>

            </Border>
            <Border Grid.Column="1"
                    Background="#1e293b"
                    CornerRadius="12"
                    Padding="18">

                <StackPanel>

                    <TextBlock Text="Password Control"
                               FontWeight="Bold"
                               FontSize="16"
                               Margin="0 0 0 10"/>

                    <StackPanel Orientation="Horizontal">

    <PasswordBox Name="PasswordHidden"
                 Width="200"/>

    <TextBox Name="PasswordVisible"
             Width="200"
             Visibility="Collapsed"/>

    <Button Content="👁"
            Width="40"
            Margin="6 0"
            Click="TogglePassword_Click"/>

</StackPanel>

                    <Button Content="Generate Password"
                            Margin="0 10"
                            Click="Generate_Click"/>

                    <CheckBox Name="DisableCheck"
                              Content="Disable Account"/>

                    <CheckBox Name="ResetCheck"
                              Content="User must reset password at next login"/>

                    <Button Content="Apply Changes"
                            Margin="0 12"
                            Click="Apply_Click"/>

                </StackPanel>

            </Border>

        </Grid>

    </Grid>

</Window>